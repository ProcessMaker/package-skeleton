import pkg from '../composer.json'; // Adjust the path as necessary
import { Meta } from "@storybook/blocks";

export const RightArrow = () => <svg 
    viewBox="0 0 14 14" 
    width="8px" 
    height="14px" 
    style={{ 
      marginLeft: '4px',
      display: 'inline-block',
      shapeRendering: 'inherit',
      verticalAlign: 'middle',
      fill: 'currentColor',
      'path fill': 'currentColor'
    }}
>
  <path d="m11.1 7.35-5.5 5.5a.5.5 0 0 1-.7-.7L10.04 7 4.9 1.85a.5.5 0 1 1 .7-.7l5.5 5.5c.2.2.2.5 0 .7Z" />
</svg>

<Meta title="Package Skeleton" />

# Package Skeleton <strong>v{pkg.version}</strong>

<div className="sb-container">
  <div className='sb-section-title'>
    ## Introduction
  </div>

  Welcome to the Package Skeleton Storybook! This interactive library is designed to showcase the components of the package.

  <div className='sb-section-title'>
    ## Getting Started
  </div>

  To start using the Package Skeleton in your ProcessMaker, follow these steps:

  Clone the repository and `cd` into the `package-skeleton` directory:

```
git clone https://github.com/ProcessMaker/package-skeleton.git
cd package-skeleton
php rename-project.php package-skeleton
composer install
npm install
npm run dev
```


## Installation
* Use `composer require processmaker/package-skeleton` to install the package.
* Use `php artisan package-skeleton:install` to install generate the dependencies.

## Navigation and testing
* Navigate to administration tab in your ProcessMaker 4
* Select `Skeleton Package` from the administrative sidebar

## Uninstall
* Use `php artisan package-skeleton:uninstall` to uninstall the package
* Use `composer remove processmaker/package-skeleton` to remove the package completely

</div>

<style>
{`
.sb-container {
  font-family: 'Arial', sans-serif;
}

.sb-section-title {
  margin-top: 20px;
  font-weight: bold;
  font-size: 20px;
}
`}
</style>
